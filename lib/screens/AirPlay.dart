import 'package:flutter/material.dart';

class AirPlay extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: new Text('airplay'),
      ),
      body:new Center(child: new Text('this is a airplay page'),)
    );
  }
}